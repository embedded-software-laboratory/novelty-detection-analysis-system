import pandas as pd
import numpy as np
from ndas.extensions import physiologicallimits


def detect_orientation_of_df(dataframe):
    x = 1


def detect_time_axis(dataframe):
    x = 1


def decide_type_for_each_df_column(dataframe, time_index):
    x = 1


def run_transform(list_of_dataframes):
    x = 1


def detect_feature_and_column(series):
    x = 1


def change_to_selected_units(dataframe):
    x = 1


def run_full_translation(list_of_dataframes):
    x = 1
